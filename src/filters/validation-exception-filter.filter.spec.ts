import { ValidationExceptionFilter } from './validation-exception.filter';

describe('ValidationExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new ValidationExceptionFilter()).toBeDefined();
  });
});
